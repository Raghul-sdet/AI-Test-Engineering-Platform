package com.enterprise.banking.ai.notification;

/**
 * Defines the visual severity of a notification (used for color coding in Slack/Teams).
 */
public enum NotificationSeverity {
    INFO,
    SUCCESS,
    WARNING,
    ERROR,
    CRITICAL
}